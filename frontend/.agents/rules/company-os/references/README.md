# References folder

`company-os/SKILL.md` references four files that belong in this folder: `departments.md`, `approval-gates-and-review.md`, `skill-gap-and-learning.md`, and `pilot-run-airbnb.md`.

As of this pass, `departments.md`, `approval-gates-and-review.md`, and `skill-gap-and-learning.md` have been recovered — they were sitting loose at the project root instead of here and have been moved in. `research-office.md` was found alongside them and moved in too; it isn't cited by an explicit `references/research-office.md` path in `SKILL.md`, but its content (the source-tier and confidence-scoring methodology) is what every department's "Sources" section and the Research Office role throughout `SKILL.md` actually depend on, so it belongs with this set.

**`pilot-run-airbnb.md` is still missing.** It was present at the project root at the start of this session but is gone now (not deleted by this session's work; its disappearance is unexplained). Copy it in from the master `company-os` skill location before treating this folder as complete, or reconstruct it deliberately with the user's confirmation. Do not let an agent fabricate a plausible-sounding version of it — that would violate the Constitution's own first rule (never invent expertise) using the very skill that rule belongs to.
