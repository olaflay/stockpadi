# Dashboard

Per-branch and consolidated view: today's sales, low-stock count, expiring-soon count, debtors owed, an unsynced-changes indicator, and a Quick Sell shortlist of frequently-sold in-stock products. Renders from IndexedDB cache first, refreshes as sync completes. Profit estimate lives in Reports, not here — see `src/features/reports`.
