# types

Shared types for anything crossing the client/server boundary — sale payloads, sync batches, stock movements — defined once here, never redefined separately on each side. Per `.agents/rules/coding-standards-and-api.md`.
