# Server configuration. Keep this file empty of real credentials.
PORT=8787
FRONTEND_ORIGIN=http://localhost:3000
NODE_ENV=development
DEV_LOG_VERIFICATION_CODES=false
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_URL=
# Public origin used by the backend compatibility adapter for manage-staff.
SUPABASE_FUNCTIONS_URL=
# Supabase secret used only by the compatibility manage-staff adapter.
BACKEND_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="StockPadi <noreply@yourdomain.com>"
